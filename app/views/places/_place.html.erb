<li>
	<%= link_to place.title, place_path(place) %>
	visited by <strong><%= place.visited_by %></strong>
	<%= link_to "Delete", place, method: :delete, data: {confirm: "Are you sure?"}, class: "btn-danger" %>
</li>
